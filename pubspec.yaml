name: flutter_shortcut_example
description: An example project showcasing the use of Flutter's native shortcuts functionality.
publish_to: none
repository: https://github.com/dartfoundry/flutter_shortcut_example
version: 1.0.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
